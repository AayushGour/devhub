# Team log
One shared log. Every agent appends 1 line per task at handoff/done.
Format: `- [agent-name] one-line summary`

- [senior-dev] Executed deck-level-config plan (type: deck block, mergeSlideStyle, footer-driven opt-in page numbers) end-to-end, Tasks 1-8: delegated T1/T2/T7 to junior-dev and reviewed clean, built T3-T6 myself, fixed a real title/body style-merge gap in SlideCard.tsx (layouts read raw config.style, now read deck-merged renderConfig), verified 158/158 tests + tsc + lint + live-browser Playwright smoke test (preview/export parity confirmed for footer/pageNumber/style inheritance and overrides).
- [manual-qa] Made SlideDeckGuide.tsx fully data-driven per user request ("data should come from a json file... auto rendered, so I just have to update the json"). New data/slideDeckGuide.json holds every tab's content (overview/types/fields/example — intro text, rules, type table, guardrails, worked example); SlideDeckGuide.tsx is now a thin generic renderer that maps over it, no hardcoded copy left in the component. Added guideInlineFormat.tsx (formatInline: supports `code` spans and **bold** in JSON strings, backtick/asterisk markers matching the SKILL.md convention) split from GuideCode.tsx (the Code chip component) to satisfy the react-refresh/only-export-components lint rule (component-only vs function-only file separation). Added resolveJsonModule to tsconfig.app.json (required for typed JSON imports). 5 new tests for formatInline (163/163 total). Verified live: all 4 tabs render identically to the pre-refactor hardcoded version (bold/code spans, type table, guardrail list, worked example all correct). Going forward, editing the guide's content is a JSON-only change — no JSX/code changes needed.
- [manual-qa] Walked back overcorrection from the earlier relative-units pass per user feedback: had converted ALL Tailwind spacing utilities (p-8, gap-3, gap-6, etc.) to arbitrary em/cqw values across the 5 slide layouts + SlideCard chrome, not just the font sizes that actually caused the preview/export scaling bug. User: keep cqw/em only where it must proportionally match between preview and export (font-size), revert spacing/padding/gap back to Tailwind's default rem-based scale where a few px of difference at different sizes doesn't matter. Reverted p-[1.8em]→p-8, gap-[0.6em]→gap-3, gap-[1.2em]→gap-6, gap-[0.5em]→gap-2, mt-[1em]→mt-4, and SlideCard's footer/badge positioning (inset-x-[2cqw]→inset-x-3, px-[0.9cqw]→px-2, gap-[1cqw]→gap-2, etc.) across TitleLayout/SectionLayout/ContentLayout/TwoColumnLayout/ImageFocusLayout/SlideCard.tsx — kept every text-[Xem]/text-[Xcqw] font-size class untouched. 163/163 tests still pass, tsc/lint clean. Verified live: title font scales 22.2px (515px preview card) → 55.3px (1280px export page), same ~2.5x ratio as the card-width ratio (the actual fix); layout padding is now a flat 32px (p-8) in both — the accepted tradeoff.
- [manual-qa] Added the full pipeline story to the in-app guide per user request: a "SKILL.md → LLM agent → markdown deck → DevHub PDF" flow visualization plus a direct SKILL.md download link, both in the Overview tab, both JSON-driven (guideData.overview.pipeline/.skillDownload — new Pipeline/SkillDownloadLink local components in SlideDeckGuide.tsx map over them, icon key strings resolve through a small PIPELINE_ICONS registry since JSON can't hold component references). SKILL.md itself now has two copies: docs/skills/slide-deck-markdown/SKILL.md (canonical, what external LLM agents/humans read from the repo) and web/public/skills/slide-deck-markdown/SKILL.md (static-asset mirror so the in-app download link has something to serve — no build step syncs them yet, flagged in a code comment, keep them in sync manually when SKILL.md changes). Download href is base-path-aware (import.meta.env.VITE_BASE_PATH, same pattern main.tsx's router basename uses) so it resolves correctly under /devhub/. 163/163 tests, tsc, lint all clean (no new tests needed — pure JSON+presentational addition, no new logic). Verified live: curl confirmed the asset serves at /devhub/skills/slide-deck-markdown/SKILL.md, the rendered flow diagram (4 steps + arrows) and download card look right, and the anchor's resolved href/download attributes are correct.
- [manual-qa] Deduped the two SKILL.md copies per user ask ("only keep one"). Replaced web/public/skills/slide-deck-markdown/SKILL.md (a literal file copy) with a symlink to the canonical docs/skills/slide-deck-markdown/SKILL.md — one file on disk, no more manual-sync burden. Verified: symlink resolves (diff shows identical content), Vite dev server serves it fine through the symlink (default fs.allow walks up to the repo root since it has its own .git/package.json, so docs/ is in bounds) — curl still returns the same 19111-byte file at /devhub/skills/slide-deck-markdown/SKILL.md. Updated the stale "keep in sync manually" code comment in SlideDeckGuide.tsx to describe the symlink instead. tsc clean.
- [manual-qa] Two content/behavior fixes per user request: (1) SKILL.md new "Media in Slides" section — prefer `graph LR`/`flowchart LR` mermaid over `TB` (TB wastes a landscape slide's horizontal space); if TB is genuinely needed, put it in a two-column slide instead of fighting the orientation. Added a matching Author Checklist item and touched up the Images/Mermaid bullets in the worked-example notes. Caught and fixed a real bug while writing the TB→two-column example: backslash-escaped backticks inside a YAML double-quoted string is wrong (backtick isn't a special YAML char, no escaping needed) — fixed to literal backticks. (2) Inline markdown images (body/columns/caption — NOT image-focus's dedicated `image` field, which was already correctly bounded by its own flex parent) had no height cap, only width (`.markdown-preview img { max-width: 100% }` from index.css) — a tall/portrait image could overflow the slide's fixed height. Added `.slide-card .markdown-preview img { max-height: 18em; object-fit: contain; width: auto }` to SlideCard.css (preview) and the matching rule to slideExport.ts's SLIDE_PAGE_CSS (export parity) — em-based (not px/vh) so it scales with the slide the same way title/body text does, riding the same cqw-derived font-size chain, no need for container-type:size/cqh. Hit and fixed a template-literal-breaking bug immediately after: a literal backtick in a comment INSIDE the SLIDE_PAGE_CSS template-literal string prematurely closed it (esbuild parse error) — removed the stray backticks from the comment. Synced the in-app guide too: new guideData.fields.media rule (JSON) + FieldsTab renders it (SlideDeckGuide.tsx). 163/163 tests, tsc, lint clean. Verified live: a 400x1600 portrait test image rendered at 166px tall inside a 290px-tall preview card (was previously unbounded/would overflow), and the new "Mermaid & Images" guide rule renders correctly in the Fields & Style tab.
